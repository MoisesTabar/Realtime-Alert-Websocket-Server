[project]
name = "realtimealertserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "pre-commit>=4.3.0",
    "pytest>=8.4.1",
]
